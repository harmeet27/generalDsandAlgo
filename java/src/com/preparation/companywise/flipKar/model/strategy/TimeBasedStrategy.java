package com.preparation.companywise.flipKar.model.strategy;

public class TimeBasedStrategy implements PricingStrategy {
    @Override
    public double computePrice(double timeFactor, int basePrice) {
        return 0;
    }
}
