package com.preparation.ds.array;

/**
 * 1. take pivot from nuts array.
 * 2. using that as pivot place the other elements of bolts array either on left (smaller than pivot) , right(> pivot)
 * 3. The final position of above pivot is fixed, using that apply step 2 on nuts array now
 * 4. Recurse these steps for 0,pivot-1 and pivot+1, end.
 */

public class SortNutsAndBolts {
}
