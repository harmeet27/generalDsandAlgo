package com.preparation.algorithm.sorting.techniques;

/**
 * 1. create an array of size : maxElement of array
 * 2. now store each value to its index with count : countingSort[arr[i]] ++;
 * 3. Now create a new array for traversal of the countingSort[] and populate element , its count times in this array.
 */
public class CountingSort {
}
